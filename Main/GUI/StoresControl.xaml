<UserControl x:Class="Main.GUI.StoresControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:myControls="clr-namespace:Main.Controls"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Main.GUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="White" Grid.Row="0">
            <StackPanel Grid.Row="0" HorizontalAlignment="Right"  VerticalAlignment="Center">
                <Button Content="Dodaj Sklep" Margin="10" Style="{StaticResource mainButton}"/>
            </StackPanel>
        </Border>

        <Border Background="White" Grid.Row="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <StackPanel x:Name="storeList"  Grid.Row="1">
             
            </StackPanel>
            </ScrollViewer>
        </Border>

    </Grid>
</UserControl>
